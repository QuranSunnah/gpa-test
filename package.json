{
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prepare": "husky",
    "fmt": "./node_modules/.bin/prettier --config .prettierrc ./*.json resources/**/*.{js,css} --write",
    "fmt-diff": "./node_modules/.bin/prettier --config .prettierrc --list-different ./*.json resources/**/*.{js,css}",
    "fix-cs": "composer fix-cs",
    "check-cs": "composer phpcs && composer test-cs"
  },
  "devDependencies": {
    "axios": "^1.7.4",
    "husky": "^9.1.6",
    "laravel-vite-plugin": "^1.0",
    "prettier": "^3.3.3",
    "vite": "^5.0"
  }
}
